FOLDER STRUCTURE:
appV/                ← main screens (Views)
  (tabs)/
    index.tsx        ← main Forecast screen
    settings.tsx     ← user settings screen
components/          ← reusable view components
  ui/
    CloudIndicator.tsx
  ForecastList.tsx
dataM/               ← model: network, parser, repository
  WeatherApi.ts
  WeatherParser.ts
  WeatherRepo.ts
hooksVM/             ← view models (state + logic)
  useWeatherViewModel.ts
  useNetworkMonitor.ts


┌────────────────────────────────────────────┐
│                USER ACTION                 │
│────────────────────────────────────────────│
│ • App launches                             │
│ • or user taps “Refresh” / opens screen    │
└────────────────────────────────────────────┘
                    │
                    ▼
        ┌────────────────────────────┐
        │   ViewModel (Hook Layer)   │
        │   useWeatherViewModel.ts   │
        └────────────────────────────┘
                    │
                    │ async function refresh()
                    ▼
      ┌──────────────────────────────────────┐
      │          Model / Data Layer          │
      │              dataM/                  │
      ├──────────────────────────────────────┤
      │ 1️⃣ WeatherApi.ts                     │
      │    → fetches JSON from Open-Meteo    │
      │      via axios/fetch                 │
      │                                      │
      │ 2️⃣ WeatherParser.ts                  │
      │    → maps raw JSON → ForecastDay[]   │
      │      [{ date, temp, cloudCover }]    │
      │                                      │
      │ 3️⃣ WeatherRepo.ts                    │
      │    → caches data in AsyncStorage     │
      │    → returns parsed ForecastDay[]    │
      └──────────────────────────────────────┘
                    │
                    ▼
      ┌──────────────────────────────────────┐
      │     useWeatherViewModel (again)      │
      ├──────────────────────────────────────┤
      │ • updates state:                     │
      │     days = ForecastDay[]             │
      │     loading = false                  │
      │     errorMessage = null              │
      │ • triggers React re-render           │
      └──────────────────────────────────────┘
                    │
                    ▼
     ┌────────────────────────────────────────┐
     │              VIEW LAYER                │
     │────────────────────────────────────────│
     │ appV/(tabs)/index.tsx                  │
     │   → uses useWeatherViewModel()         │
     │   → passes vm.days → ForecastList      │
     │                                        │
     │ components/ForecastList.tsx            │
     │   → maps each ForecastDay to:          │
     │       • date text                      │
     │       • <CloudIndicator percent=…>     │
     │       • temperature text               │
     │                                        │
     │ components/ui/CloudIndicator.tsx       │
     │   → renders circle w/ gray intensity   │
     │     ∝ cloudCover percentage            │
     └────────────────────────────────────────┘
                    │
                    ▼
        ┌────────────────────────────┐
        │         USER UI            │
        │  🕶️  Visual list of days   │
        │  🌤️  CloudIndicator icons  │
        │  🌡️  Temperatures          │
        └────────────────────────────┘

At Runtime: 

1️⃣ User opens app → HomeScreen mounts
2️⃣ useWeatherViewModel() triggers vm.refresh() → fetches from API
3️⃣ getForecast() → returns array of ForecastDay objects
4️⃣ vm.days updates → React re-renders ForecastList
5️⃣ Each ForecastDay renders:

Day name (Mon, Tue, …)

Cloud indicator (☀️, ⛅, ☁️)

Temperature number

Result: a scrollable 7-day visual forecast.